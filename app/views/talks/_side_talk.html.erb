<div class="side-bar-header">
  <div class="header-name">
    <div class="login-user">
      <ul class="login-user-lists">
        <li class="background-red" id="lists"><%= "#{current_user.name}" %></li>
      </ul>
      <ul class="show-lists hidden" id="pull-down">
        <li class="pull-down-list"><%= link_to "編集", edit_user_path(current_user) %></li>
        <li class="pull-down-list"><%= link_to "詳細情報", user_path(current_user) %></li>
        <li class="pull-down-list"><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
      </ul>
    </div>
  </div>
  <%= image_tag "gray-logo.png", class: "side-logo__img" %>
  <div class="create-room">
    <%= link_to "メンバーを作成する", new_room_path %>
  </div>
</div>

<div class="side-user-index">
  <div class='user-contents'>
    <% User.where(id: @room.users.ids).each do |user| %>
    <table class="table-talk">
      <%= image_tag user.image, class: :card_img  if user.image.attached? %>
      <tbody>
        <tr>
          <th class="table__col1">名前</th>
          <td class="table__col2"><%= user.name %></td>
        </tr>
      </tbody>
    </table>
    <% end %>
  </div>
</div>